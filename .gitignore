# Go
/bin/
*.test
*.out
coverage*

# locally built command binaries (from `go build ./cmd/...`)
/harbor
/pilot
/pilot.exe
/gateway

# ADR 0003 Phase 2: the embedded nebula asset is fetched at build time
# (`make embed-nebula`), platform-specific, ~8 MB gzipped — never committed.
internal/nebulaboot/assets/

# M0 spike runtime artifacts (generated certs, keys, configs, logs, pids)
spike/m0/run/
# locally built tools (e.g. pkcs11-enabled nebula-cert)
spike/m0/tools/

*.log
*.pid

# editor / OS
.DS_Store
*.swp

# local SQLite dev databases
*.db
