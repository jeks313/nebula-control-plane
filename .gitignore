# Go
/bin/
*.test
*.out
coverage*

# locally built command binaries (from `go build ./cmd/...`)
/harbor
/harbor.exe
/pilot
/pilot.exe
/gateway
/nebula-boot

# ADR 0003 Phase 2: the embedded nebula asset is fetched at build time
# (`make embed-nebula`), platform-specific, ~8 MB gzipped — never committed.
internal/nebulaboot/assets/

# M0 spike runtime artifacts (generated certs, keys, configs, logs, pids)
spike/m0/run/
# locally built tools (e.g. pkcs11-enabled nebula-cert)
spike/m0/tools/

*.log
*.pid

# editor / OS
.DS_Store
*.swp

# local SQLite dev databases
*.db

# local, real deployment identifiers (bucket / account / instance): keep the repo public
# while deploying to your own environment. Copy the committed *.example templates to fill in.
/deploy/prod/env.local
/deploy/prod/terraform/app/backend.hcl
/deploy/prod/terraform/foundation/backend.hcl

# terraform local state + working dirs (never commit)
*.tfstate
*.tfstate.*
.terraform/
