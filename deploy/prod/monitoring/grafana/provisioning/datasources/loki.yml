# Grafana datasource provisioning — Loki (control-plane logs, ADR 0007 Phase 7c).
apiVersion: 1
datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    editable: false
